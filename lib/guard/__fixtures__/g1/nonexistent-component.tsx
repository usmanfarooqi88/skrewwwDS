// A genuine Skrewww-path claim (real component-directory import shape)
// naming a component that does not exist in the canonical registry —
// the one real violation shape for component/nonexistent-slug.
import { CommandPalette } from "@/components/ui/CommandPalette";

export function Example() {
  return <CommandPalette />;
}
